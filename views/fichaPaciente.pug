extends layout
block content
    h2 Perfil do Utente #{paciente.nome}
    div(class='triagem')
        form(method="POST" action="/triagem/setPrioridadeUser/" + paciente.NUS)
            label(id="nus" utente=paciente.NUS) NUS: 
                span #{paciente.NUS}
            label Data de Nascimento: 
                span #{paciente.dataNascimento}
            label Genero: 
                span #{paciente.genero}
            label Morada: 
                span #{paciente.rua} , #{paciente.concelho} , #{paciente.distrito} , #{paciente.pais}
            label Estado do Paciente
            | Pulseira Verde
            input(type="radio", name="paciente_status", value="Verde")
            | Pulseira Amarela
            input(type="radio", name="paciente_status", value="Amarelo")
            | Pulseira Vermelha
            input(type="radio", name="paciente_status", value="Vermelho")
            | Fazer Exames
            input(type="radio", name="paciente_status", checked="true" value="Exame")
            label Observações Feitas
            textarea(name="done", cols="40", disabled=true, rows="5") #{descricao}
            label Observações
            textarea(name="descricao", cols="40", rows="5")
            button(id="submit" name="Submeter" value="Submeter") Submeter
        a(href="/triagem") Voltar atrás
