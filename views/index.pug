extends layout
block content
    form(method="POST" action="/users/login")
        label
            | Utilizador
            input(type="text" id="username" name="username")
        label
            | Palavra-Passe
            input(type="password" name="password")
        button(name="login" id="login" value="login") Login
    script
      include ../utils/scripts/validatorLogin.js